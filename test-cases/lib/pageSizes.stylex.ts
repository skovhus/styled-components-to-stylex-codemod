import * as stylex from "@stylexjs/stylex";

export const PageSizeConstants = stylex.defineConsts({
  listInitiativeRowHeight: 58,
});

export const CardSizeConstants = stylex.defineConsts({
  cardWidth: "10",
});
