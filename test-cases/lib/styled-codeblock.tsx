import styled from "styled-components";

/** A direct styled-components export — its CSS is unlayered. */
export const Codeblock = styled.pre`
  margin: 0;
  padding: 8px;
  overflow: auto;
  white-space: pre;
  background: #f6f8fa;
  font-family: monospace;
`;
