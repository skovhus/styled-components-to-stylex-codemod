import * as stylex from "@stylexjs/stylex";

export const $interaction = stylex.defineConsts({
  /** Condition key: true when the device supports hover (pointer device). */
  canHover: "@media (hover: hover)",
});
