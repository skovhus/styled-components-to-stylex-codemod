output:
  - summary
  - success
  - failure

pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{json,css,yaml,yml,html,md,graphql,js,jsx,ts,tsx}"
      run: pnpm lint:fix {staged_files}
      stage_fixed: true
